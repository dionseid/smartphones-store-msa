eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

global-message: Configuration server is alive!

management:
  info:
    env:
    enabled: true
  endpoints:
    web:
    exposure:
      include: health,info
